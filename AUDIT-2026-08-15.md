# point-bbs 全面审计报告（2026-08-15）

审计范围：后端 7 个 Maven 模块（point-domain / point-db / point-cache /
point-service / point-web / point-admin / point-boot，共 117 个 Java 文件）、
前端 Vue 3 SPA（41 个源文件）、3 份 JSON 配置。审计方式：3 个并行子代理
（后端安全 / 后端正确性 / 前端）逐文件审查 + 主代理交叉验证 + 运行时回归测试。

---

## 一、freeway 依赖升级（任务 1）

**结论：最新 SNAPSHOT = `1.3.8-SNAPSHOT`（freeway HEAD `b6ff921`，2026-08-14 18:53），
point-bbs 已在上一提交（25e7f15）升级到该版本线。**

- 已核实：freeway 本地仓库 HEAD == origin/main == `b6ff921`；无更新的 SNAPSHOT
  （tags 最高 v1.3.6-SNAPSHOT；m2 中央仓库最高 1.3.7-SNAPSHOT；本地 m2 已有
  HEAD 级构建）。
- 本次重新构建并安装了 freeway HEAD 到本地 m2（`mvn install -Dgpg.skip=true`，
  跳过仅发布用的 GPG 签名），确认解析到的是 HEAD 级 API（`readBodyLimited` 桥
  接已移除、`WebSocketRoute` 仅 2 参构造等）。
- point-bbs 全部 7 个模块在 HEAD 级 freeway 下**零改动编译通过**；
  测试 `TopicServiceTest` 3/3 通过；启动 581ms，全量 API 冒烟通过。

### 适配中发现并修复的真实缺陷（新词法器行为差异）

**`ESCAPE '\'` 与方言词法冲突（搜索 500）** —— `TopicRepository` 的标题搜索
`LIKE $keyword ESCAPE '\'` 在 MySQL 方言词法器（H2 `MODE=MySQL` URL 自动派生
MySqlDialect）下，`\'` 被当作转义引号，字符串永不闭合，`$limit/$offset` 被吞掉，
报 `Unknown named parameter 'offset'`（/api/topics/search 返回 500）。
更深入的调查发现三方互斥：

| 消费者 | 对 `ESCAPE` 字面量的要求 |
|---|---|
| freeway MySQL 方言词法器 | `'\\'`（否则吞 SQL） |
| H2 `MODE=MySQL` 运行时 | `'\'`（`'\\'` 是双字符转义符 → 22025 报错） |
| 真实 MySQL 运行时 | `'\\'`（`'\'` 引号被转义 → 语法错误） |

**修复**：改用跨方言无歧义的转义字符 `!` —— `ESCAPE '!'` +
`keyword.replace("!", "!!").replace("%", "!%").replace("_", "!_")`。
H2 与 MySQL 均接受，词法器无歧义（含注释说明）。

---

## 二、审计发现与修复汇总

### 🔴 严重（Critical）

1. **【前端】markdown 图片网格路径存储型 XSS** —— `src/utils/markdown.ts`
   手工拼接 `<img src="${src}" alt="${alt}" />` 未做转义/校验。复现：
   两行连续图片 + `![a](x onerror=alert\`1\`)` → 输出
   `<img src="x onerror=alert\`1\`" alt="a" />`，`v-html` 渲染时
   `onerror` 触发（模板字符串调用无需括号）。覆盖主题/文章/评论所有 `v-html`
   面（MomentCard / TopicDetail / ArticleDetail）。结合 localStorage 中的
   JWT，可完整盗号。
   **修复**：三层防护 —— `md.validateLink()` 协议白名单（挡
   `javascript:`/`data:`/无协议垃圾）+ 空格百分号编码（空格才是 HTML 属性
   分隔符，仅转义引号不够）+ `escapeHtml`。已用 Node 复现脚本验证 payload
   被中和。

2. **【后端】粉丝/关注列表泄露密码哈希** —— `/api/users/{id}/followers` 与
   `/following` 直接返回 `SELECT u.*` 的完整 User 实体（含 password 字段）。
   **修复**：`sanitizePublic()` 置空 password/email/phone；公开资料
   `/api/users/{id}` 同样脱敏。运行验证：响应中 password/email/phone 均为 null。

### 🟠 高（High）

3. **【后端】管理端禁言/解禁是静默空操作** —— `AdminUserRoutes` 对游离实体
   `setForbiddenEndTime` 后调用 `updateUser`（只写 6 个资料字段），禁言时间戳
   从未落库。
   **修复**：新增 `UserService.setForbiddenEndTime(userId, endTime)` 直写
   `forbidden_end_time`；404 处理。运行验证：禁言后 signin 被拒（"用户已被禁言"）。

4. **【后端】无管理员可用（开箱即用管理面板 100% 不可用）** —— data-seed 只建
   权限/角色，从不给任何用户分配角色；前端/路由均无授勋口。
   **修复**：(a) data-seed 增加配置驱动的引导管理员
   （`bbs.admin.username` + `bbs.admin.password`，无 admin 权限时创建并授角色，
   默认配置为空 → 无操作）；(b) dev-data-seed 幂等地把 墨客（用户 1）授为
   管理员（已存在数据库也生效）。运行验证：moke 可访问 /api/admin/*。

5. **【后端】计数维护破损** —— 评论删除不减父实体 `comment_count`、不减用户
   `comment_count`；主题删除不减 `topic_count`；评论创建从不加用户
   `comment_count`；种子数据插入 12 条评论/10 个赞但实体计数为 0。
   **修复**：CommentService.delete 事务化并回减（父实体 + 用户，GREATEST 防负）；
   CommentService.create 增加用户计数；TopicService.delete 事务化并回减
   topic_count；dev seed 补 topic/article 的 comment/like 计数修复 SQL。
   运行验证：删评论后 topic.commentCount 1→0，删主题后 topicCount 3→2。

6. **【后端】`getRecentTopics` 分页 total 与列表过滤不一致** —— items 过滤
   `type=0 AND status=1`，total 却是全表 COUNT（含已删/动态）。
   **修复**：新增 `countByType(0)`。

7. **【后端】并发 check-then-insert 竞态** —— like/favorite/follow 无唯一约束，
   并发双击可重复插入并双倍计数。
   **修复**：5 张表加唯一复合索引（`@Index(unique=true)`：UserLike、Favorite、
   UserFollow、ThirdUser、User.username/email）；服务层捕获唯一键冲突按幂等处理。
   **注意**：H2 下 freeway 的 MySQL 方言索引内省（INFORMATION_SCHEMA.STATISTICS）
   不可用导致 Schema.ensure 跳过建索引 —— data-seed 按 `jdbc:h2` URL 直接
   `CREATE UNIQUE INDEX IF NOT EXISTS`（已运行验证 6 个 UQ_* 索引全部落库）；
   真实 MySQL 由 Schema.ensure 创建。

8. **【后端】密码哈希为单轮 SHA-256+salt** —— 离线可爆破。
   **修复**：升级为 PBKDF2WithHmacSHA256（120k 迭代，JDK 内置零依赖）；
   兼容旧 `salt:hash` 格式并在登录成功时透明升级（运行验证：moke 登录后已升级）。

9. **【后端】JWT 缺陷** —— 签名比较非恒定时间；默认密钥占位符公开。
   **修复**：`MessageDigest.isEqual` 恒定时间比较；payload 转义补 \r/\u2028/\u2029；
   占位符密钥下 prod profile 启动即失败、非 prod 打醒目警告。

10. **【后端】上传面** —— topicId `Long::parseLong` 与 Base64 解码裸奔 500；
    任意文件类型可上传并以 inline 同源展示（HTML/SVG 存储型 XSS 面）；文件名
    未净化（Content-Disposition 头注入面）；下载整文件读入内存；getFilePath
    O(n) 目录扫描。
    **修复**：上传仅收 `image/*`（ServiceException 400）；文件名净化
    （路径分隔符/引号/控制字符替换，截断 128）；下载按类型
    inline/attachment 分流 + `X-Content-Type-Options: nosniff`；
    getFilePath 直接解析（保留扫描兜底）。运行验证：`topicId=abc` → 400 JSON。

11. **【后端】OAuth 回调开放重定向** —— 用请求 `Origin` 头拼跳转 URL。
    **修复**：改为从配置的 `redirect-uri` 推导 SPA origin；`state` 校验仍缺失
    （需 cookie/session 基建，列入遗留项）。

12. **【后端】已删主题仍可访问** —— 详情路由用无 status 过滤的 findById。
    **修复**：详情路由 `status != 1 → 404`。运行验证：删除后详情 404。

13. **【前端】MomentCard 评论响应形状不匹配** —— 后端返回
    `{items,page,pageSize,total}`，前端按数组读，评论面板渲染垃圾行、
    加载更多失效。
    **修复**：读 `payload.items`、按 `total` 算 hasMore。

14. **【前端】上传 Promise 永不落定** —— FileReader onload 里 async 抛错无
    reject，外层 await 永久挂起、spinner 卡死。
    **修复**：reader 分离 resolve/reject + try/catch + finally。

15. **【前端】ArticleDetail 加载无错误处理/无路由 watch** —— 404 无限转圈、
    /articles/1 → /articles/2 显示旧内容。
    **修复**：loadArticle 重构（try/catch/finally + `watch(route.params.id)`）。

16. **【后端】8+ 路由 bodyAsJson 空体 NPE → 500**（UserRoutes 编辑/已读、
    ArticleRoutes 评论、AdminCategory/AdminConfig）。
    **修复**：全部补空体守卫返回 400。

17. **【后端】ArticleService 局部编辑清空字段** —— update() 无条件写全部列，
    只传 title 会置空 summary/content/cover/sourceUrl。
    **修复**：按非空字段动态 SET；tags 非空才更新。

18. **【后端】ArticleService.create SELECT-after-INSERT 竞态取错行**。
    **修复**：用 INSERT 生成键。

### 🟡 中（Medium）

19. 登录/注册无速率限制 → 新增 `LoginRateLimiter`（滑动窗口，每 IP 10 次/10 分钟，
    signin/signup 生效，429 响应）。运行验证：第 11 次起 429。
20. signup/signin 未走 freeway 类型化 body 校验（@NotBlank/@Size 全部失效）→
    改为 `Route.post(path, Class, handler)`；前端登录/注册页展示校验错误明细。
    运行验证：缺 email/短密码均 400 校验错误。
21. 事件订阅者异常会回滚业务事务 → ServiceModule 全部 try/catch（通知尽力而为）。
22. unlike/unfollow/favorite 非事务 → 全部包 `db.transaction`。
23. 错误响应泄露异常类名（"服务器内部错误: SqlException"）→ 改为通用文案。
24. 整数 OFFSET 溢出（7 处 `int offset = (page-1)*pageSize`）→ 全部 long。
25. 裸 pageSize 未钳制（/following、/moments、/recommended、文章列表）→
    PageRequest 钳制或 limit 封顶 50。
26. 收藏列表 IDOR（任意用户可读他人收藏）→ 改为仅本人；新增
    `POST /users/{id}/messages/read-all` 路由（前端"全部已读"原来发空 ids 是
    空操作）。
27. 文章 like 响应恒 `{"liked": true}` → 返回真实结果。
28. 前端登录后 user 状态不完整（只存 signin 响应的 4 字段）→ store 登录/注册
    后 `await fetchCurrentUser()` 拉全量。
29. Messages/Favorites 挂载竞态（App.vue 异步取用户前发
    `/users/undefined/...`）→ watch 等待 user 就绪。
30. 路由守卫缺失/不一致 → 集中 `beforeEach`（requiresAuth meta，7 个路由），
    补 scrollBehavior。
31. 乐观更新无 try/catch（TopicDetail/ArticleDetail）→ 补 catch 与
    likeCount 防负。
32. AdminTopicRoutes accept_answer `Long.parseLong` 裸奔 → 400 守卫。
33. 附件下载大小无上限/无认证（score 恒 0）→ 维持现状，列入遗留。
34. Category type 文档与种子不一致（normal vs free）→ 实体默认值与注释对齐
    seed；TopicTag.status long→int。

### 🔵 低 / 信息（Low / Info，已修或已记录）

35. 【已修】SearchPage 不响应 `?q=` 变化 → watch route.query.q。
36. 【已修】FavoritesPage 文章收藏跳 /topics → 按 entityType 跳。
37. 【已修】MentionTextarea 防抖 timer 未清理 → onUnmounted 清理。
38. 【已修】UserProfile isSelf 用 username 路由时失效 → id 或 username 双比较。
39. 【已修】seed QA 分类/内容类型瑕疵、dev 计数器补齐。
40. 【记录】CORS `*`（dev 默认，prod 已收紧）；OAuth state 未校验（需
    cookie 基建）；JWT 存 localStorage（建议 httpOnly cookie + refresh）；
    token 经 query 参数传递（OAuth 回调需要，会入浏览器历史）；
    事件总线 8 种事件无订阅者、20+ 实体表无代码（bbs-go 遗留 schema）；
    评论回复 API 缺失（前端"回复"实际是普通评论）；`findSticky` 无 LIMIT；
    附件下载无鉴权/积分；`roles` 列死字段；`following`/`moments` 返回裸数组
    （与分页信封不一致，前端已适配）；AdminFilter 依赖硬编码 canonical ID。

### ✅ 审计确认为干净的部分

- SQL 注入：全部 $named/`?` 参数化，IN 子句构建器与表名 switch 均白名单，
  LIKE 已用 `ESCAPE '!'` —— 无注入点。
- 越权：用户侧变更类路由（主题/评论/文章编辑删除、资料编辑、消息、关注）
  全部有属主校验；AdminFilter 从 DB 重算权限（伪造 roles 声明无效）。
- N+1：ResponseEnricher 列表批量 IN 查询，无 N+1。
- ScopedValue 请求隔离正确；SimpleCache 线程安全（computeIfAbsent + TTL）。
- 前端 v-for key、监听器清理、图片查看器、401 拦截无循环。

---

### 补充修复（安全子代理复核后追加）

41. 公开资料再脱敏：`birthday` / `homePage` / `roles` 一并置空（原仅
    password/email/phone）。运行验证：/api/users/2 六字段全为 null。
42. `UserRepository.searchByPrefix`（@提及搜索）LIKE 通配符未转义 →
    改用 `ESCAPE '!'` + `!` 转义（与 TopicRepository 同约定）。
    运行验证：`q=%` 返回空（字面匹配）。
43. 管理端 topic/user 列表分页未钳制 → 统一走 `PageRequest`（≤100）。
    运行验证：`pageSize=99999` → 100；`pageSize=-5` → 20。

---

### 优化轮（2026-08-15 追加）

44. **SVG 上传封堵（安全）**：`image/svg+xml` 通过 `image/*` 检查但内联直览
    可执行脚本 → 上传拒绝 + 下载按 attachment 分流。
45. **全局安全响应头**：新增 `SecurityHeadersFilter`（CSP self-only +
    `X-Frame-Options: DENY` + nosniff + `Referrer-Policy: same-origin`），
    位于 spa-filter 之前（SPA 路由也生效）。运行验证：`/`、静态资源、API
    均携带。
46. **权限码缓存**：AuthFilter 每请求 3 表 JOIN → `PermissionService` 60s TTL
    `SimpleCache`，assignRole/removeRole 显式失效。
47. **前端包体 -82%**：Vuetify 全量导入 → `vite-plugin-vuetify` 按需自动导入 +
    vendor 分包。`index` 主包 722KB → 110KB（gzip 234KB → 41KB），chunk 警告
    消除；vue-tsc 类型检查通过。
48. **依赖对齐 freeway HEAD**：slf4j 2.0.17 → 2.0.18，H2 2.3.232 → 2.4.240
    （既有 2.3 数据文件可正常打开，迁移 43 项通过，启动 450ms）。
49. **前端同步脚本**：`tools/sync-frontend.sh`（构建 → 同步 static →
    提示重打包），消除手工 cp 遗漏。

### 15 项优化清单（2026-08-15 全部完成）

| # | 项目 | 实现 |
|---|---|---|
| 1 | OAuth state 校验 | httpOnly SameSite=Lax cookie 存 state，回调比对后销毁（登录 CSRF 关闭） |
| 2 | 评论回复语义 | quoteId>0 → 父评论 comment_count+1 + "comment" 事件通知被回复者（激活原死代码分支） |
| 3 | WebSocket 通知推送 | /ws/notify（token 或 cookie 认证）+ NotificationHub + 4 类事件 ping；前端接帧刷新未读数，轮询兜底。已验证 @提及 → 对端收 `{"type":"refresh"}` |
| 4 | 管理后台前端 | /admin 四 Tab（帖子/用户/分类/配置）+ requiresAdmin 路由守卫 + 侧边栏入口 + `/users/current/permissions` 端点 |
| 5 | 自动化测试 | AuthServiceTest(7)/UserServiceTest(6)/CommentServiceTest(3) 新增，共 19 例全绿；`tools/smoke-api.sh` 16 项端到端冒烟。**测试抓到 AuthService 手写 JSON 解析的转义 bug（含引号昵称损坏）→ 改用 JsonUtils 修复** |
| 6 | CI 流水线 | .github/workflows/ci.yml：JDK25+Node22，sonatype-snapshots 仓库配置，后端测试+前端构建+lint+冒烟 |
| 7 | ESLint+Prettier | flat config + typescript-eslint + eslint-plugin-vue + globals + editorconfig；0 error（38 条 any 警告留作存量债）；prettier 已全库格式化 |
| 8 | 前端类型债务 | PageResult/AuthResult/ApiEnvelope/Message 类型；8 个视图的 any ref 全部 typed |
| 9 | 列表信封统一 | /following /moments /recommended /articles 全部 {items,page,pageSize,total}，前端消费方同步 |
| 10 | 死代码清理 | 5 个未发布事件类、User.roles 死列、12 个未用 repository/service 方法移除；sealed permits 同步 |
| 11 | slf4j 日志 | PointModule/WebModule/AuthService/ServiceModule 的 System.out/err → Logger |
| 12 | 搜索评估 | 保持 LIKE+ESCAPE '!'（BBS 规模足够；数据量上来后换 H2/MySQL FULLTEXT，已记录） |
| 13 | 浏览量节流 | TopicService 每主题 60s 窗口去重写库 |
| 14 | JWT→httpOnly Cookie | 完整迁移：signin/signup/OAuth 回调 Set-Cookie(HttpOnly+SameSite=Lax，Domain 可配)；AuthFilter/WS 端点读 cookie；signout 清 cookie；前端去 localStorage、sessionChecked 启动探测、守卫等待；SameSite=Lax 天然防 CSRF。dev 用 Domain=localhost 兼容 Vite 代理。已验证 cookie 鉴权/管理/WS/登出全链路 |
| 15 | 附件系统 | 上传大小上限（`bbs.upload.max-size` 默认 10MB）；下载流式输出（`output(InputStream,long)`）不再整读入内存 |

**测试新增发现并修复**：AuthService 手写 JSON 提取不处理转义 —— 昵称含 `"`/`\` 时 token 往返损坏（AuthServiceTest.nicknameEscapingSurvivesRoundTrip 捕获）。

### 数据结构层优化（2026-08-15 追加）

**缺陷修复**
- `last_comment_time` 评论创建/删除后从不更新（帖子不会因新评论上浮），
  `last_comment_user_id` 从未写入 → 评论创建时 touch（时间+用户），删除时
  COALESCE 回退到最新存活评论的时间。
- `Message.extraData` 冒号拼接（"topic:142"）解析脆弱 → 新消息写 JSON
  （`{"type":"topic","id":142}`），前端双格式兼容解析。
- 内容无长度上限（title 超 128 截断/DDOS 面）→ DTO @Size（title≤128、
  content≤100k、comment≤20k、article≤200k），ArticleRoutes 评论创建转
  typed-body 让校验生效；服务层枚举校验（topic type∈{0,1,2}、
  comment entityType∈{topic,article}）。

**索引（性能）**
- 13 个高频查询列加实体 `@Index`（topic user/category/list/created、
  comment entity/user、message user+status、article user/status、
  tag 关联、third_user user、attachment topic）。
- **H2 切换 PostgreSQL 模式**：URL `MODE=MySQL`→`MODE=PostgreSQL`（dev+测试）。
  派生 PostgresDialect → H2 原生索引内省 → Schema.ensure 自动建全部索引，
  MySQL 方言内省警告消失，手动 DDL 钩子从 20 条缩到 1 条（idx_follow_other，
  @Index 不可重复无法实体声明）。老库（MySQL 模式建、列名大写）可正常打开；
  `bbs_sys_config.key` 改裸标识符（NON_KEYWORDS 覆盖，避免引号大小写失配）。

**表治理**
- 启用两个日志表：管理写操作审计（AdminAudit → bbs_operate_log：删帖/推荐/
  置顶/采纳/禁言/分类/配置）与附件下载日志（bbs_attachment_download_log）。
- 21 张无代码死表移出 ALL_ENTITIES（Vote*/CheckIn/Badge*/LevelConfig/
  TaskConfig/Dict*/SmsCode/Email*/UserReport/ForbiddenWord/UserFeed/
  UserToken/UserExpLog/UserScoreLog/UserTask*/Link）——表保留不再迁移，
  启用功能时加回。

**验证**：老库+PG 模式全 API 200、写操作正常；新库全流程（21 表+索引+seed+
admin 授勋）成功；19/19 测试、16/16 冒烟、启动 0 告警。

**完全适配 PostgreSQL（生产目标 MySQL → PG）**
- `application-prod.json`：`jdbc:postgresql://localhost:5432/bbs`，移除强制
  `dialect`（URL 自动派生 PostgresDialect）。
- `point-boot` 增加 `org.postgresql:postgresql` 42.7.7（runtime）。
- 6 处 `@Column(type="LONGTEXT")` → 方言中立 `TEXT`（PG 无限长；MySQL
  不再作为目标）。
- **真实 PostgreSQL 18.4 端到端验证**（rootless 解包二进制起实例）：
  Schema.ensure 原生建 21 表 + 20 索引（14 查询 + 6 唯一 + idx_follow_other
  钩子）；seed/授勋/登录全流程；冒烟 16/16；评论删除 GREATEST/COALESCE、
  审计日志、JSON 通知、50k 字符 TEXT 长文全部通过。期间发现并修复
  `@了你` 通知 extraData 仍为旧冒号格式（→ JSON）。
- 开发默认仍是 H2 `MODE=PostgreSQL`（零配置开箱），dev 与 prod 统一
  Postgres 方言族。

---

## 三、剩余遗留项（未修，建议后续）

1. OAuth `state` 防登录 CSRF —— 需要 cookie/session 或前端回传机制。
2. 密码策略/防爆破（账号级锁定、验证码）—— 现仅有 IP 级限流。
3. 附件系统完整化（鉴权、积分扣费、流式下载、大小上限）。
4. 死代码清理：20+ 无代码实体表、8 种无订阅事件、`User.roles` 死列、
   未用 repository 方法。
5. 前端类型债务：大量 `any`、缺 `PageResult<T>` 类型、`UserInfo` 未覆盖
   signin 响应。
6. 评论区"回复"语义（现为带 quoteId 的普通评论）。
7. 生产部署指引：JWT 密钥注入、`bbs.admin` 引导、CORS 收紧、
   MySQL 保留字列（`key`/`value`/`label`/`type`/`level`/`index`）的
   NON_KEYWORDS H2 特供问题。
